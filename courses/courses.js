let st={q:'',c:'הכל',l:'הכל',o:'default'},g=document.getElementById('allCoursesGrid'),r=document.getElementById('resultCount'),q=document.getElementById('courseSearch'),o=document.getElementById('sortCourses');
const n=v=>parseInt(String(v||'').replace(/\D/g,''),10)||0;
function draw(){let a=C.filter(x=>{let z=st.q.trim().toLowerCase(),txt=(x.t+' '+x.c+' '+x.cs+' '+x.l).toLowerCase();return(!z||txt.includes(z))&&(st.c==='הכל'||x.c===st.c||x.cs.split('|').includes(st.c))&&(st.l==='הכל'||x.l===st.l)});if(st.o==='title')a.sort((a,b)=>a.t.localeCompare(b.t,'he'));if(st.o==='students')a.sort((a,b)=>n(b.s)-n(a.s));if(st.o==='views')a.sort((a,b)=>n(b.v)-n(a.v));r.textContent=a.length+' קורסים';g.innerHTML=a.map(x=>`<a class="course-card" href="https://trade2.co.il/courses/" target="_blank" rel="noopener"><div class="course-img"><img src="${x.i}" alt="${x.t.replace(/"/g,'&quot;')}" loading="lazy"></div><div class="course-body"><div class="card-top"><span class="tag">${x.c}</span>${x.l?`<span class="level">${x.l}</span>`:''}</div><h3>${x.t}</h3><div class="course-meta">${x.d?`<span>◷ ${x.d}</span>`:''}${x.s?`<span>👥 ${Number(x.s).toLocaleString('he-IL')}</span>`:''}</div></div></a>`).join('')||'<div class="empty">לא נמצאו קורסים שמתאימים לחיפוש.</div>'}
document.querySelectorAll('[data-category]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-category]').forEach(x=>x.classList.remove('active'));b.classList.add('active');st.c=b.dataset.category;draw()});
document.querySelectorAll('[data-level]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-level]').forEach(x=>x.classList.remove('active'));b.classList.add('active');st.l=b.dataset.level;draw()});
q.oninput=e=>{st.q=e.target.value;draw()};o.onchange=e=>{st.o=e.target.value;draw()};draw();document.querySelectorAll('[data-current-year]').forEach(e=>e.textContent=new Date().getFullYear());